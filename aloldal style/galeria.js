document.addEventListener("DOMContentLoaded", function () {
    const kolodkoCarousel = new bootstrap.Carousel("#kolodkoCarousel", {
        interval: 3000,
        wrap: true
    });

    const szobrokCarousel = new bootstrap.Carousel("#szobrokCarousel", {
        interval: 3000,
        wrap: true
    });
});
